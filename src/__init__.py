"""
ml.src — reusable, importable modules for dataset discovery, models,
training, evaluation, and XAI. Notebooks under ml/notebooks/ import from
here rather than duplicating logic, so the same code runs identically
whether it's called from a notebook or a script.
"""
